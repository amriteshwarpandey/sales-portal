# Where the Express API runs (used by the /api rewrite in next.config.mjs)
API_URL=http://localhost:5000
# Socket.IO endpoint the browser connects to for real-time updates
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
